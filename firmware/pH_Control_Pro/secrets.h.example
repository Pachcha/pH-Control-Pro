/*
 * secrets.h.example
 * ----------------------------------------------------------------------------
 *  คัดลอกไฟล์นี้เป็น "secrets.h" ในโฟลเดอร์เดียวกัน แล้วใส่ค่าจริงของคุณ
 *
 *      cp secrets.h.example secrets.h
 *
 *  ★ secrets.h ถูกใส่ไว้ใน .gitignore แล้ว จะไม่ถูก commit ขึ้น GitHub
 *    ห้ามใส่รหัส WiFi จริงลงในไฟล์ .example นี้เด็ดขาด
 * ----------------------------------------------------------------------------
 */
#pragma once

// ───── WiFi ─────
#define WIFI_SSID       "ชื่อ-WiFi-ของคุณ"
#define WIFI_PASSWORD   "รหัสผ่าน-WiFi"

// ───── Firebase ─────
// หาได้จาก Firebase Console:
//   API key      : Project settings > General > Web API Key
//   Database URL : Realtime Database > หน้าแรก (ลิงก์ด้านบนตาราง)
//                  แนะนำสร้างที่โซน asia-southeast1 (สิงคโปร์) จะเร็วที่สุดในไทย
#define FIREBASE_API_KEY       "AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
#define FIREBASE_DATABASE_URL  "https://ชื่อโปรเจกต์-default-rtdb.asia-southeast1.firebasedatabase.app"
